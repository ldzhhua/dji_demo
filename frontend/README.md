# Frontend (Vue 3)

This directory is intended for the Vue 3 application that will provide a user interface for dataset management, model training, and inference visualization.

To get started:

```bash
npm install -g @vue/cli
vue create frontend-app
cd frontend-app
npm run serve
```

This will create a new Vue 3 project. You can then integrate API calls to the FastAPI backend.
